return {
  -- "nvim-neo-tree/neo-tree.nvim",
  -- opts = {
  --   filesystem = {
  --     filtered_items = {
  --       visible = false,           -- 保持过滤掉的项目完全隐藏（而不是显示为灰色）
  --       hide_dotfiles = true,      -- 核心配置：隐藏以 . 开头的文件和文件夹
  --       hide_gitignored = false,   -- 核心配置：不隐藏 .gitignore 中的内容
  --       hide_hidden = true,        -- （可选）隐藏系统级别的隐藏文件（主要针对 Windows）
  --     },
  --   },
  -- },
}
